# Tasks: Create Hugo Homepage

1.  **Scaffold Styles**: Create `assets/css/main.css` implementing the CSS variables from `design.md` (colors, fonts, spacing).
2.  **Define Layouts**: Create `layouts/_default/baseof.html` and `layouts/index.html` with the semantic HTML structure.
3.  **Implement Grid**: Build the CSS Grid layout in `main.css` supporting Desktop (multi-column) and Mobile (stack).
4.  **Create Card Component**: unique `partials/components/bento-card.html` that accepts title, content, and "size" (span) classes.
5.  **Build Hero Section**: Implement the primary "Introduction" card.
6.  **Build Project Feed**: Create a loop in `index.html` to render `bento-card`s for the latest "featured" projects.
7.  **Build Skills/Stats**: Create static or data-driven cards for Skills and other Bento widgets (like "Time in Melbourne").
8.  **Build Footer**: Implement the minimal social icon footer.
9.  **Review & Polish**: Ensure animations (hover states) and responsive breakpoints work as intended.
