# Tasks: Polish Design

- [x] 1. **Global Ambience**: Update `main.css` `body` with a radial gradient background to create depth (vignette effect).
- [x] 2. **Card Aesthetics**: Refine `.c-card` to use a subtle top-down linear gradient surface (lighter at top `rgba(255,255,255,0.03)` to transparent).
- [x] 3. **Hero Spotlight**: Add a specific modifier class `.c-card--hero` with a rich, colorful gradient background (e.g., deeply blurred purple/blue orb).
- [x] 4. **Typography Polish**: Update `.text-display-xl` and headers to match the "tight tracking" and weights from `design.md`.
- [x] 5. **Button Glow**: Update `.btn` styles to have a subtle glow/shadow on hover.
- [x] 6. **Stat Card**: Style the "Years Building" card to match the "Insight" card inspiration (Green/Gradient background or accent).
